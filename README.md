# Operación A2 · V10

V10 parte de la V9 y mantiene las mismas claves de almacenamiento local (`a2tests`, `a2history` y `a2drafts`), por lo que no borra el historial ni el progreso guardado.

Novedad: el importador detecta automáticamente el número de preguntas del simulacro (hasta 90), siempre que estén numeradas consecutivamente, tengan opciones A/B/C/D y exista una plantilla de respuestas al final.
